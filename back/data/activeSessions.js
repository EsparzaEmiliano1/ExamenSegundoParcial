// Este arreglo guarda las sesiones activas
// Cada sesión tendrá el token y el id del usuario.
// Ejemplo: [ { token: "abc-123", userId: 1 }, { token: "xyz-789", userId: 2 } ]

const activeSessions = [];

module.exports = activeSessions;